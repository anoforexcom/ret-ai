// Este ficheiro já não é necessário na versão simplificada (Client-side).
// A lógica foi movida de volta para services/geminiService.ts
export default function handler() {
  return new Response("Not used", { status: 404 });
}